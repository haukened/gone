tools:
    - name: eslint
        enabled: true
    - name: semgrep
        enabled: true
        version: 1.78.0

configuration:
    eslint:
        # Use project .eslintrc.json so modern browser/ES2022 rules apply
        configFile: .eslintrc.json
